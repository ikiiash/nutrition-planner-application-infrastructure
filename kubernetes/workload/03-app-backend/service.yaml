apiVersion: v1
kind: Service
metadata:
  name: nutrition-planner-be
  namespace: app
  labels:
    app: nutrition-planner-be
spec:
  selector:
    app: nutrition-planner-be
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
